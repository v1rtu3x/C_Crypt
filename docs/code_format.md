# Code Formatting Policy

C_Crypt uses `clang-format` for automatic formatting.

Formatting rules:

- 4-space indentation
- no tabs
- Allman brace style
- 100-column line width
- one declaration per line
- related header first in `.c` files
- deterministic include ordering
- files must end with a trailing newline
